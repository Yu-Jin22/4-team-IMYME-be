spring.application.name=mine

# ==================================================
# DB (PostgreSQL)
# ==================================================
# ???? ??, ??? ??? ?? (?? ???)
# - CI/CD: GitHub Secrets?? DB_URL, DB_USERNAME, DB_PASSWORD ??
# - ??: .env ?? ?? ??? ??
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/mine_project_db}
spring.datasource.username=${DB_USERNAME:mine}
spring.datasource.password=${DB_PASSWORD:imymemine}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==================================================
# JPA / Hibernate
# ==================================================
# ddl-auto=update: ??? ?? ? DB ??? ?? ????
spring.jpa.hibernate.ddl-auto=update

# ???? SQL? ??? ??
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# OSIV ???? (?? ?? ?? ?? ??)
spring.jpa.open-in-view=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==================================================
# ?? ???
# ==================================================
# CommandLineRunner? ?? ??? ?? (DataInitializer.java)
# data.sql ?? Java ??? ?? ??? ??
